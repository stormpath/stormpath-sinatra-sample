<h1><%= group.name %></h1>

<h4>Group Accounts</h4>

<table class="table">
  <thead>
    <tr>
      <th>Username</th>
      <th>Email</th>
    </tr>
  </thead>
  <tbody>
  <% group_accounts.each do |account| %>
    <tr>
      <td><%= account.username %></td>
      <td><%= account.email %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<h4>Custom Data Permissions</h4>
<table class="table">
  <thead>
    <tr>
      <th>Can view classified information</th>
      <th>Can delete others</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= group_custom_data.get('view_classified_info') ? "TRUE" : "FALSE" %></td>
      <td><%= group_custom_data.get('delete_others') ? "TRUE" : "FALSE" %></td>
    </tr>
  </tbody>
</table>

<h4> Update Permissions </h4>
<form accept-charset="UTF-8" action='<%= "/directories/#{CGI.escape(directory.href)}/groups/#{CGI.escape(group.href)}" %>' method="post"><input name="utf8" type="hidden" value="✓"><input name="_method" type="hidden" value="put">
  <label for="View_Classified_Info">View classified info</label>
  <input id="view_classified_info" name="view_classified_info" type="text" value="<%= group_custom_data.get('view_classified_info')%>">
  <label for="Delete_Others">Delete others</label>
  <input id="delete_others" name="delete_others" type="text" value="true" value="<%= group_custom_data.get('view_classified_info')%>">
  <br>
  <input name="commit" type="submit" value="Change Permissions">
</form>